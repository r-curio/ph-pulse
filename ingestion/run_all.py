"""PH-Pulse ETL runner — executes all ingestion sources."""


def main() -> None:
    """Run all ingestion pipelines."""
    print("PH-Pulse ingestion pipeline — no sources configured yet.")


if __name__ == "__main__":
    main()
